1. Para instalar json-server:

npm install -g json-server

2. Para ejecutar json-server:

json-server --watch publicaciones.json

json-server --port=8101 --host=192.168.1.87 --watch publicaciones.json              